- Add more logging information to the remote-cache server to help debug issues with the cache. This includes logging the request method, URL, and headers for each request received by the server. Additionally, log the response status code and any errors encountered while processing the request.
- Make sure that the caching system understands nested directories and can cache files in subdirectories correctly. Sub-workspaces should automatically have their files ignored by the super-workspace, and the caching system should be able to handle files in nested directories without any issues. This may involve updating the caching logic to recursively traverse directories and cache files accordingly.
- When staring a "ciabatta run", I should show the summation of dependencies in the terminal: env variables, targets to be built
- A "target" should be able to define what files it is dependent on, and what env variables it's dependent on. This will allow for more fine-grained control over the build process and ensure that targets are only rebuilt when necessary. The caching system should take these dependencies into account when determining whether a target needs to be rebuilt or can be retrieved from the cache.
- Any given target should be able to track the following:
  - The files it depends on (inputs)
  - The files it produces (outputs)
  - The environment variables it depends on
  - The commands it runs to produce its outputs
  - Any other targets it depends on (dependencies)
  - What team owns the target

- Make sure I can see all these dependencies in the web viewer, using a toggle so that I can see the full dependency tree for any given target. This will help me understand how targets are related to each other and how changes to one target may affect others. The web viewer should provide a clear and intuitive interface for exploring the dependency graph and visualizing the relationships between targets.